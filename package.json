{
  "name": "auth-handlers",
  "version": "1.0.0",
  "description": "Protocol-specific authentication handlers for OAuth, SAML, OIDC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run build && node --test tests/*.test.cjs"
  },
  "keywords": [
    "auth",
    "oauth",
    "saml",
    "oidc",
    "handler"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "jose": "^5.2.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.3.0"
  }
}
